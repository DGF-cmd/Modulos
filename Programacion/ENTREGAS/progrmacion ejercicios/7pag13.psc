Proceso if0
	escribir "escribe numeros o algo"

	Mientras x1<>99 y x2<>99 Hacer
		leer a
		leer b
		leer c
			x1=(-b)+(raiz(b^2 + 4*a*c))/2*a;
			x2=(-b)-(raiz(b^2 + 4*a*c))/2*a;
			Escribir "Las respuestas son " x1, " y " x2 
		FinMientras
		

	
FinProceso
	